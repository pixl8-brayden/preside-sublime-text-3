<snippet>
	<content><![CDATA[
component {
	private function index( event, rc, prc, args={} ) {
		return renderView(
			  view          = 'page-types/${1:page_types}/index'
			, presideObject = event.getCurrentPageType()
			, id            = event.getCurrentPageId()
			, args          = args
		);
	}
}
]]></content>
	<tabTrigger>handler</tabTrigger>
	<scope>embedding.cfml</scope>
	<description>Preside Handler Base</description>
</snippet>
